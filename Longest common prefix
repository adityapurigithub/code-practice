/**
 * @param {string[]} strs
 * @return {string}
 */
var longestCommonPrefix = function(strs) {
    let ans=strs[0];

    for(let i=1;i<strs.length;i++){
        while(!strs[i].startsWith(ans)){
            ans=ans.slice(0,-1);
        }
    }


    return ans;
};
